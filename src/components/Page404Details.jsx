import styled from "styled-components"

const Page404Details = styled.div`
    .error_description {
        text-align: center;
        padding: 5rem 0;

        h1 {
            color: var(--blue);
        }
    }
`;
export default Page404Details;
