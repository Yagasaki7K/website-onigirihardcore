import styled from 'styled-components';

const ModalDetails = styled.div`
    display: flex;

    button {
        img {
            width: 1rem;
            margin-right: 0.5rem;
        }
    }

`

export default ModalDetails
